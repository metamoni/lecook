<%= form_for @recipe, html: { class: 'form' } do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, class: 'recipe-title' %><br />
  <%= f.label :instructions %>
  <%= f.rich_text_area :instructions %>
  
  <%= f.submit 'Save recipe', class: 'button success-button' %>
<% end %>
